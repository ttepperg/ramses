AUTHOR: Dr. Thorsten Tepper Garcia

CREATED: 30 JUL 2025

    This file is a log intended to keep track and guide me through the process of updating my Ramses fork and patches in response to the significant changes made to the main Ramses repo. See TTG_README in my fork's main dir.

    This file replaces bck_29JUL2025/README. See the latter for more information.

    STEPS COMPLETED
    - Backed up old patch files in bck_29JUL2025 (back dated for consistency with modifications to other of my patches)
    - Copied relevant files from ramses_fork/path/tepper/dice
    - Modified the relevant files as outlined in bck_29JUL2025/README


    USAGE (*):

    In the NML, use:

    &RESTART_PARAMS
    nrestart2=N			! restart at snapshot N
    add_dice_ic=.true.  ! optional; to load additional DICE ICS at restart
    restart_vars=6,7   	!-> corresponds to the actual indices of passive scalars (excluding temperature); mandatory when hydro = .true.
    /

    &INIT_PARAMS
    filetype='ramses_dice'
    /

    &RUN_PARAMS
    nrestart=0
    /

    (*) See patch/tepper/namelist/dice_restart.nml and dice_restart_gas.nml


    KNOWN BUGS

        - DOES NOT WORK when star-formation is included:
        Restarting a simulation (h_00_gh0_lr_ofe_wh_rs) with the oagertz/dice_restart patch (which derives from this), leads to the mass of evolved, newborn stellar particles drop below their allowed minimum value.
        -> TO BE CHECKED AGAIN AND FIXED ASAP

        - Main step starts at 1 instead of at the actual last value at restart; the same is true for nstep_coarse.
        -> TO BE CHECKED AGAIN AND FIXED IF NEEDED

    	- When loading additional DICE ICs, there may be a potential issue with duplicate particle ids (iord)
        -> TO BE CHECKED AGAIN AND FIXED ASAP

    	- Gas particle (cell) number and gas mass increases slightly at restart, presumably due to the fact that IG_rho != 0
        -> TO BE CHECKED AGAIN AND FIXED IF NEEDED

    	- Particles at or beyond the box boundary will be tossed at restart
        -> TO BE CHECKED AGAIN AND FIXED IF NEEDED


    TO DO:

    	- Remove obsolete bits such as uu for particles [what else?]


    Last Modified 30 JUL 2025:

        - SO FAR NOTHING ELSE TO DO APPARENTLY; STILL TESTING THOUGH
        - Tested with model dice_restart, compiled with  Makefile_3D_GNU_OLDMPI0_DICE_RESTARTMULTIPATCH
            - tested normal run
            - tested normal restart run
            - tested dice_restart run
                - changing number of cpus
                - adding ICs at restart

-----------------------------------------
MODIFIED FILES


    read_params.f90:
    - added module 'dice_restart_commons' (called restart_commons pre JUL2025)
    - added subroutine 'read_dice_restart_params()', which includes the declaration of the NML block dice_restart_params (called restart_params pre JUL2025).
    IMPORTANT: Note that this requires to update all relevant NML files in patch/tepper/namelist -> TO DO
    - added the strings '! DICE patch' and '! DICE_RESTART patch' to several locations to distinguish between the snippets particular to each of thee patches
    - added verification of some dice_restart_params parameters to subroutine read_run_params()


    init_part.f90:
    - added subroutine load_ramses_dice and corresponding:
        case ('ramses_dice')
            call load_ramses_dice

    - added to subroutine load_dice the following:
        use restart_commons, only: restart_init
    and some related checks (look for dice_restart_init)
