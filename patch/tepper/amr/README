Author
    Dr. Thorsten Tepper Garcia

Created
    12 MAY 2020

    Last Modified:

    25 AUG 2026
    Updated read_params.f90 to the latest changes in the main Ramses repo

    18 AUG 2026
    Updated output_amr.f90, read_params.f90 to the latest changes in the main Ramses repo

    22 MAY 2026
    Updated output_amr.f90 to the latest changes in the main Ramses repo

    01 MAY 2026
    Updated amr_parameters.f90, output_amr.f90, read_params.f90 to the latest changes in the main Ramses repo

    02 MAR 2026
    Updated amr_parameters.f90, read_params.f90 to the latest changes in the main Ramses repo

    26 FEB 2026
    Updated read_params.f90 to the latest changes in the main Ramses repo

    5 AUG 2025
    - Added read_params.f90 to patch (see below)

    29 JUL 2025:
    Replaced all files as a result of updating my Ramses fork and patches in response to the significant changes made to the main Ramses repo. See TTG_README in my fork's main dir. The previous patched files have been moved to the directory bck_29JUL2025.
    Added detail information about the modifications needed (see below).
    Tested with model hbd_10, compiled with  Makefile_3D_GNU_OLDMPI0_DICEMULTIPATCH
    Tested with model hbd_10_gd0, compiled with  Makefile_3D_GNU_OLDMPI0_DICEMULTIPATCH_GAS


About

    This subdirectory contains a number of patches
    to files originally in amr, unless stated otherwise.

Usage
    N/A


Notes
    N/A


To-DO
    N/A


Bugs
    N/A


Modifications
** Note that not all modifications are detailed as they are, but they can be easily searched for with the information provided

    amr_parameters.f90:
    - added output parameter 'output_dir' (type character(len=80))
     which allows to redirect the output to any location


    init_amr.f90:
    - added TRIM(output_dir) to strings fileloc;
     look for 'output_'//TRIM(nchar)


    movie.f90:
    - added TRIM(output_dir) to string moviedir
     look for 'output_'//TRIM(nchar)


    output_amr.f90:
    - added TRIM(output_dir) to filedir
     look for 'output_'//TRIM(nchar)
    - added afer line 'call backup_part(filename, filename_desc)' the following:
        if(sink)then
           filename=TRIM(filedir)//'sink_'//TRIM(nchar)//'.csv'
           call output_sink_csv(filename)
        end if


    units.f90:
    - added factG_in_cgs, M_sun, Myr2sec, kpc2cm from amr/constants.f90
    - added hydro, mass_sph from hydr/hydro_parameters.f90
    - added new variables:
        real(dp)::scale_m
        real(dp),parameter::km2cm=1.0d+5
        logical,save::first_call=.true.
    - automatically set scale_t when poisson=.true. to ensure that G=1 in user units for all gravity runs
    - added:
        ! mass conversion factor
        scale_m = scale_d * scale_l**3
    - added an output block with relevant scaling information


    read_params.f90:
    - added variable 'output_dir' to namelist block 'output_params'
    - added TRIM(output_dir) to info_file strings where appropriate
    - added variable 'print_when_io' to namelist block 'run_params'
